describe('PageConfig', () => {
    it('should change the time spent', () => {
        //this should test whether or not the method is called. 
    });

    it('should save something to storage', () => {
        //
    });
});
